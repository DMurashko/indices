import React from 'react';

import IndexGroup from '../IndexGroup/IndexGroup';

export default function OtherIndex({ indices }) {
	return (
		<IndexGroup 
			title="OtherIndex"
			indices={indices}
		/>
	);
}