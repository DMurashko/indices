import React from 'react';

import IndexGroup from '../IndexGroup/IndexGroup';

export default function VenturePortfolioIndex({ indices }) {
	return (
		<IndexGroup 
			title="VenturePortfolioIndex"
			indices={indices}
		/>
	);
}